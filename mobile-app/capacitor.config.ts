import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.keepit.mobile",
  appName: "KeepIt",
  webDir: "dist/client",
  android: {
    backgroundColor: "#F7F5F1",
  },
};

export default config;
